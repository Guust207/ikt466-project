# ikt466-project

## Relevant files for the project
We did not make time to clean the repository so we feel a note should be made on which files are relevant to run the model:
* cnn/gustav_temp.ipynb - Used for running default model
* cnn/gustav_temp3.ipynb - Used for running K-Fold
* preprocessing/wav_to_waveform.py - Used to convert.wav files to .png files.
